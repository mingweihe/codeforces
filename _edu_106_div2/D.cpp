#include <bits/stdc++.h>

using namespace std;

const int maxn = 2e5+1;
int n, k, a[maxn];

void solve(){
    cin >> n >> k;
    for(int i = 0; i < n; ++i){
        cin >> a[i];
    }
    int ans = 0;
    
    cout << ans << '\n';
}

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    int t;
    cin >> t;
    while(t--){
        solve();
    }
}
